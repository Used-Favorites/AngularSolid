import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.angular.solid',
  appName: 'Used Favorites',
  webDir: 'dist/browser',
};

export default config;
